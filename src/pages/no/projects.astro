---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
const projects = await getCollection("projects_no");
---
<BaseLayout title="Prosjekter" lang="no">
  <h2>Prosjekter</h2>
  <ul>
    {projects.map((p) => (
      <li>
        <a href={`/no/projects/${p.slug}/`}>{p.data.title}</a><br/>
        <small>{p.data.year}</small><br/>
        {p.data.summary}
      </li>
    ))}
  </ul>
</BaseLayout>
